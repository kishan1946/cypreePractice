{
  "name": "automationpractice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run-chrome": "cypress run --browser chrome",
    "cy:run-chrome-head": "cypress run --headed --browser chrome",
    "cy:run-headed": "cypress run --headed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.1.0"
  }
}
